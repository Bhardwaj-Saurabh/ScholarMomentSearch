# Web
fastapi>=0.110
uvicorn[standard]>=0.27
python-dotenv>=1.0

# Manifest (Neon Postgres) + work queue (Prefect Cloud)
psycopg[binary,pool]>=3.1
prefect>=3.0

# Object storage (S3 protocol: AWS / GCS-interop / Tigris + GCS native SDK)
boto3>=1.34
google-cloud-storage>=2.16     # STORAGE_PROVIDER=gcp_native

# Vector DB
qdrant-client>=1.12

# Visual embeddings (CLIP) — pulls torch
sentence-transformers>=2.7
pillow>=10.0
numpy>=1.24

# Transcript (text) branch — bge semantic embeddings, ONNX (no torch), tiny
fastembed>=0.3

# Paper & deck parsing (Assignment 3) — page/slide-aware text extraction
pymupdf>=1.24
python-pptx>=0.6.23

# Video acquisition (YouTube path). Keep this CURRENT — YouTube changes its
# bot-detection constantly and old yt-dlp versions stop working first.
yt-dlp>=2026.7.4

# Bring-your-own multimodal LLM (optional providers; install what you use)
openai>=1.30
# anthropic>=0.25   # uncomment for LLM_PROVIDER=anthropic

# Cache (DESIGN.md §3d) — query-embedding/frame/answer caching. redis-py talks
# to redis-stack-server for both plain get/set (components 19-21) and
# RediSearch vector search (component 22's semantic answer cache).
redis>=5.0

# Auth0 access-token validation (DESIGN.md §3f). [crypto] pulls `cryptography`,
# which PyJWT needs for RS256 — the plain install cannot verify Auth0 tokens.
pyjwt[crypto]>=2.8

# RAG tracing (DESIGN.md §3g). BOTH optional — src/tracing.py imports them
# lazily and degrades to "no backend" if absent, so the app runs without them.
# opik pulls ~26 transitive packages (incl. litellm); the OTel trio is light.
opik>=2.2
opentelemetry-sdk>=1.25
opentelemetry-exporter-otlp-proto-http>=1.25
